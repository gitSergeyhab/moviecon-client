import { FC } from "react";

const Footer: FC = () => {
  return (
    <footer className="bg-base-gradient fixed w-full bottom-0  py-2 text-center font-bold shadow-xl min-w-80">
      moviecon@2024
    </footer>
  );
};

export default Footer;
