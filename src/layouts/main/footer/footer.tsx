import { FC } from "react";

const Footer: FC = () => {
  return (
    <footer className="bg-basic-gradient fixed w-full bottom-0 text-white py-2 text-center font-bold shadow-xl min-w-80">
      moviecon@2024
    </footer>
  );
};

export default Footer;
