import { FC } from "react";

const Footer: FC = () => {
  return (
    <footer className="fixed bottom-0 bg-base-gradient w-full min-w-80 py-2 text-center font-bold shadow-xl">
      moviecon@2024
    </footer>
  );
};

export default Footer;
